# Claude Code Integration Sample

1. Copy `.env.example` to `.env` and set `ANTHROPIC_API_KEY`.
2. Run in terminal:

```bash
npm install
npm start
```

3. `index.js` sends a simple prompt to Claude Code (`claude-code-v1`) and prints completion.
